server.port=8761

#telling the server not to register himself in the service
eureka.client.register-with-eureka=false
#Eureka clients fetch the service registry (ServiceInstance: {URL, PORT, HOST}) from the Eureka server if we set it as true.
eureka.client.fetch-registry=true